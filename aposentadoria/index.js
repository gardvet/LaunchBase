const nome = 'Hildegard'
const idade = 44
const sexo = 'M'
const tempocontrib = 12

const aposentar = (idade + tempocontrib)

if (aposentar >= 95) {
    console.log(`${nome}, você está aposentado`)
}else {
    console.log(`${nome}, você não está aposentado`)
}